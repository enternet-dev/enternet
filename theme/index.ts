import { Transition } from './Animations';
import { BorderColor, BorderRadius } from './Borders';
import { Color } from './Colors';
import { Spacing } from './Spacings';
import { FontSize, LineHeight } from './Fonts';

// Global style should be the last import of the list.
import { GlobalStyle } from './GlobalStyle';

export {
  Transition,
  BorderColor,
  BorderRadius,
  Color,
  GlobalStyle,
  Spacing,
  FontSize,
  LineHeight,
};
